import { SafeAreaView } from 'react-native-safe-area-context';
import TabViewExample from 'src/layouts/tab-view-basic';

const OrderScreen = (props: any) => {
  return <TabViewExample></TabViewExample>;
};

export default OrderScreen;
